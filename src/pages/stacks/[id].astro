---
import Layout from "../../layouts/Layout.astro";
import StackDetails from "../../react/StackDetails";
const { id } = Astro.params;
---

<Layout title="Stack Details">
  <StackDetails client:only="react" stackId={id || ""} />
</Layout>
